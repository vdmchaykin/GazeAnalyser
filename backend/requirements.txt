fastapi==0.136.1
uvicorn[standard]==0.46.0
pandas==2.2.3
numpy==2.2.5
aiosqlite==0.21.0
pupil-apriltags>=1.0.4
opencv-python-headless>=4.8.0
scipy>=1.11

# Vendored pupil detector (app/vendor). CUDA 11.8 build: it runs on machines
# without an NVIDIA GPU too — torch falls back to CPU — at the cost of ~3 GB.
--extra-index-url https://download.pytorch.org/whl/cu118
torch==2.7.1+cu118
torchvision==0.22.1+cu118
